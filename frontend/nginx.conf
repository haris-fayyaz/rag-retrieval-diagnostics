server {
    listen 8443;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        # SPA fallback: serve index.html for any path not found on disk
        # This is the fix for BrowserRouter + Docker we discussed earlier
        try_files $uri $uri/ /index.html;
    }
}